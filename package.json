{
  "name": "idle-ants",
  "version": "1.0.0",
  "description": "An idle game about ants collecting food",
  "main": "src/index.js",
  "scripts": {
    "start": "http-server -o -c-1",
    "build": "node build.js",
    "start:dev": "http-server index.html -o -c-1",
    "start:prod": "npm run build && http-server index-production.html -o -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
} 